{
  "name": "@effikit/mcp-server",
  "version": "1.0.0",
  "description": "Effikit MCP Server — turns the Effikit toolkit into a discovery engine any MCP-capable AI can reason over.",
  "type": "module",
  "private": true,
  "bin": {
    "effikit-mcp": "./server.ts"
  },
  "scripts": {
    "start": "tsx server.ts",
    "dev": "tsx watch server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
